[postgres]
directory = .
command   = postgres -D /usr/local/var/postgres

[redis]
directory = .
command   = redis-server

[web]
directory = .
command   = bundle exec passenger start -p 5000

[worker]
directory = .
command   = bundle exec sidekiq -c 1
